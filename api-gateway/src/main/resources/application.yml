spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: bff-service
          uri: ${BFF_URL:http://localhost:8084}
          predicates:
            - Path=/api/**

server:
  port: ${API_GATEWAY_PORT:8080}
